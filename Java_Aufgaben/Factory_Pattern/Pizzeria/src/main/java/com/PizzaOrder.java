package main.java.com;
// import javax.swing.JComboBox;
// import javax.swing.JFrame;
// import javax.swing.JLabel;
// import javax.swing.JPanel;
// import javax.swing.BoxLayout; // added code

// import java.awt.Component; // added code

public class PizzaOrder {

    

    public static void main(String[] args){
        // PizzaStore store = new RostockPizzaStore();
        // Pizza pizza = store.orderPizza("Salami");
        // pizza.getClass();

        // PizzaStore store;
        // Pizza pizza;

        // JFrame frame = new JFrame("A Simple GUI");
        // frame.setVisible(true);
        // frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
        // frame.setSize(500, 500);
        // frame.setLocation(430, 100);
    
        // JPanel panel = new JPanel();
        // panel.setLayout(new BoxLayout(panel, BoxLayout.Y_AXIS)); 
        // frame.add(panel);
    
        // JLabel lbl = new JLabel("Wähle dein Pizzeria");
        // lbl.setAlignmentX(Component.CENTER_ALIGNMENT);
    
        // panel.add(lbl);

        // String[] pizzeria_choices = { "Hamburg", "Rostock", "Berlin"};
                             
        // final JComboBox<String> cb = new JComboBox<String>(pizzeria_choices);
    
        // cb.setMaximumSize(cb.getPreferredSize());
        // cb.setAlignmentX(Component.CENTER_ALIGNMENT);
        // panel.add(cb);

        // JLabel lbl2 = new JLabel("Wähle deine Pizza");
        // lbl2.setAlignmentX(Component.CENTER_ALIGNMENT);
        // panel.add(lbl2);

        // String[] pizza_choices = { "Salami", "Hawaii", "Calzone", "QuattroStagioni"};
                             
        // final JComboBox<String> cb2 = new JComboBox<String>(pizza_choices);
    
        // cb2.setMaximumSize(cb2.getPreferredSize());
        // cb2.setAlignmentX(Component.CENTER_ALIGNMENT);
        // panel.add(cb2);
        
        // String pizzeria = cb.getSelectedItem().toString();
        // String s_pizza = cb2.getSelectedItem().toString();



        // switch(pizzeria){
        //     case "Hamburg": store = new HamburgPizzaStore();
        //     case "Rostock": store = new RostockPizzaStore();
        //     case "Berlin": store = new BerlinPizzaStore();
        // }

        // switch(s_pizza){
        //     case "Salami": pizza = store.orderPizza(s_pizza);
        //     case "Calzone": pizza = store.orderPizza(s_pizza);
        //     case "QuattroStagioni": pizza = store.orderPizza(s_pizza);
        //     case "Hawaii": pizza = store.orderPizza(s_pizza);
        // }

        // frame.setVisible(true); // added code
        
    }  
}
